[package]
name = "don"
version = "0.1.0"
edition = "2024"
description = "Boss of your dev environment"
license = "MIT"
repository = "https://github.com/pjtatlow/don"


[dependencies]
ansi-to-tui = "7"
axum = "0.8"
bollard = "0.20"
bytes = "1"
crossterm = { version = "0.28", features = ["event-stream"] }
bzip2 = "0.5"
flate2 = "1"
clap = { version = "4", features = ["derive"] }
futures-util = "0.3"
glob = "0.3"
libc = "0.2"
hyper = "1"
hyper-util = { version = "0.1", features = ["tokio", "server", "server-auto", "http1"] }
tower = "0.5"
tower-service = "0.3"
nix = { version = "0.29", features = ["signal", "process", "fs"] }
notify = "8"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream"] }
pty-process = { version = "0.5", features = ["async"] }
quick-xml = "0.39"
ratatui = { version = "0.29", default-features = false, features = ["crossterm", "unstable-rendered-line-info"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
tar = "0.4"
tokio = { version = "1", features = ["full", "test-util"] }
xz2 = "0.1"
zip = "2"
zstd = "0.13"
tokio-stream = "0.1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"

# Enforce no-panics-in-production as a build-time constraint.
# Test modules opt out via `#[allow(clippy::unwrap_used, ...)]`.
[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
todo = "deny"
unimplemented = "deny"
dbg_macro = "deny"
# Library code should never println — OutputManager owns stdout.
# main.rs opts out for legitimate CLI output.
print_stdout = "deny"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
