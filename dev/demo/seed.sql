-- Watched by the demo stack's `seed` task. Touch this file to move the task
-- into pending_run — it's the quickest way to get the UI's `*` flag on screen.
insert into users (name) values ('ada', 'bda');
